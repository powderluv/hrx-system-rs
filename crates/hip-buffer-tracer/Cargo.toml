[package]
name = "hip-buffer-tracer"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
hip-function-table = { path = "../hip-function-table" }
libc = "0.2"

[lib]
name = "hip_buffer_tracer"
crate-type = ["cdylib"]
